name: 'Release Drafter'

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  update_release_draft:
    name: Release
    uses: chukysoria/workflows/.github/workflows/new-release.yml@v1.0.15
